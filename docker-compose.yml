version: '3.8'

services:
  autogenie:
    build: .
    ports:
      - "8501:8501"  # Streamlit
      - "8000:8000"  # FastAPI
    volumes:
      - .:/app
      - /app/.venv
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    env_file:
      - .env